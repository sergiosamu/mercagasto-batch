# Configuración para Render PostgreSQL
# =================================

# Opción 1: Usar DATABASE_URL (recomendado para Render)
# La URL la proporciona Render automáticamente
DATABASE_URL=postgresql://usuario:password@host:puerto/database

# Opción 2: Variables individuales (alternativa)
DB_HOST=your-render-postgres-host.render.com
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
DB_SSLMODE=require
DB_CONNECT_TIMEOUT=30
DB_APP_NAME=mercagasto-batch

# Gmail API (mismo que local)
GMAIL_CREDENTIALS=credentials.json
GMAIL_TOKEN=token.pickle

# Configuración de procesamiento
BACKUP_DIR=/tmp/ticket_backups
MAX_RETRIES=3

# Configuración específica para producción
LOG_LEVEL=INFO
ENVIRONMENT=production

# Variables adicionales para Render
# (Render las configura automáticamente)
# PORT=10000
# RENDER=true